<?xml version="1.0"?>
<launch>
  <!-- Public names are approved, but full-system composition remains blocked until
       required runtime implementations, packet contracts, timeouts and TF gates are verified. -->
</launch>
